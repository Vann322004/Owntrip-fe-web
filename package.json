{
  "name": "owntrip-fe-web",
  "private": true,
  "scripts": {
    "build": "cd admin && npm run build",
    "build:admin": "cd admin && npm run build",
    "build:hotel": "cd hotel-owner && npm run build"
  }
}
